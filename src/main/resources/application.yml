spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_to_project
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${MAIL_USERNAME}
    password: ${MAIL_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
        starttls:
          enable: true
cloud:
  aws:
    credentials:
      accessKey: AKIAU6GDWNCHBIIRV4XK
      secretKey: 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
    region:
      static: eu-north-1
    stack:
      auto: false
application:
  bucket:
    name: intitajavaproject
server:
  port: 8080
spring-doc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html